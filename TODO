- support ranges (a bit complicated, 'Accept-Ranges: none' is sent until then)
- support If-Modified-Since (simply use mdate of first chunk for this)
- get all the HTML out of index.php and put it in a separated template
- support Unicodes filenames using mbstring functions
